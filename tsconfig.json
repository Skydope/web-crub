{
  "extends": "astro/tsconfigs/strict",
  "include": [
    ".astro/types.d.ts",
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.astro"
  ],
  "tailwindCSS": {
    "config": "./tailwind.config.cjs" // Ruta explícita
  },
  "exclude": [
    "dist"
  ]
}